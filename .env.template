# Google Cloud Project ID
# Required for API quota and access. 
# Can also be provided via CLI flag --project
GOOGLE_CLOUD_PROJECT=your-project-id-here

# Target Google Cloud Region
# The script will verify model availability in this region.
# Defaults to 'us-central1' if not set.
# Can also be provided via CLI flag --region
REGION=europe-west4
